{
  "name": "zachparent-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro build && wrangler pages dev",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "astro build && wrangler pages deploy dist --project-name=zachparent-site",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.2.1",
    "astro": "^6.1.9"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "wrangler": "^4.85.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
